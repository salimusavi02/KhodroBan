# راهنمای تست اتصال به Supabase

## ✅ بررسی تنظیمات

قبل از تست، مطمئن شوید که در فایل `.env` این مقادیر تنظیم شده‌اند:

```env
VITE_BACKEND_TYPE=supabase
VITE_SUPABASE_URL=https://zwrzokyzjwircrhrtyyi.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

## 🧪 مراحل تست

### 1. راه‌اندازی سرور توسعه

```bash
cd frontend
npm run dev
```

### 2. باز کردن مرورگر

به آدرس `http://localhost:5173` بروید (یا پورت دیگری که Vite استفاده می‌کند)

### 3. تست ثبت‌نام (Register)

1. به صفحه `/register` بروید
2. اطلاعات زیر را وارد کنید:
   - **نام**: هر نامی که می‌خواهید
   - **ایمیل**: یک ایمیل معتبر (مثلاً `test@example.com`)
   - **رمز عبور**: حداقل 6 کاراکتر
3. روی دکمه "ثبت‌نام" کلیک کنید

**نتیجه مورد انتظار:**
- ✅ اگر موفق باشد: به صفحه Dashboard هدایت می‌شوید
- ❌ اگر خطا بدهد: پیام خطا نمایش داده می‌شود

### 4. تست ورود (Login)

1. به صفحه `/login` بروید
2. ایمیل و رمز عبوری که ثبت‌نام کردید را وارد کنید
3. روی دکمه "ورود" کلیک کنید

**نتیجه مورد انتظار:**
- ✅ اگر موفق باشد: به صفحه Dashboard هدایت می‌شوید
- ❌ اگر خطا بدهد: پیام خطا نمایش داده می‌شود

### 5. بررسی در Supabase Dashboard

1. به [Supabase Dashboard](https://supabase.com/dashboard) بروید
2. پروژه خود را انتخاب کنید
3. به بخش **Authentication → Users** بروید
4. باید کاربر جدید را ببینید

### 6. بررسی در Console مرورگر

1. در مرورگر، `F12` را بزنید تا Developer Tools باز شود
2. به تب **Console** بروید
3. باید پیام زیر را ببینید:
   ```
   Active Backend Type: supabase
   ```
4. اگر خطایی وجود داشته باشد، در Console نمایش داده می‌شود

## 🔍 بررسی مشکلات احتمالی

### مشکل 1: خطای "کاربر لاگین نشده است"

**علت:** Supabase session از بین رفته است

**راه‌حل:**
- دوباره لاگین کنید
- یا در Console مرورگر `localStorage.clear()` را اجرا کنید

### مشکل 2: خطای "Invalid API key"

**علت:** `VITE_SUPABASE_ANON_KEY` اشتباه است

**راه‌حل:**
- از Supabase Dashboard → Settings → API → Project API keys
- `anon` key را کپی کنید و در `.env` قرار دهید
- سرور dev را restart کنید

### مشکل 3: خطای "Email confirmation required"

**علت:** Email confirmation در Supabase فعال است

**راه‌حل:**
- به Supabase Dashboard → Authentication → Email Auth بروید
- "Enable email confirmations" را غیرفعال کنید
- یا ایمیل تأیید را از inbox خود بررسی کنید

### مشکل 4: خطای "Table not found"

**علت:** Migration ها اجرا نشده‌اند

**راه‌حل:**
- به [راهنمای Supabase Setup](../supabase-setup.md) مراجعه کنید
- Migration ها را اجرا کنید

## 📊 تست کامل‌تر

برای تست کامل‌تر، می‌توانید:

1. **تست CRUD برای Vehicles:**
   - یک خودرو اضافه کنید
   - آن را ویرایش کنید
   - آن را حذف کنید

2. **تست CRUD برای Services:**
   - یک سرویس اضافه کنید
   - لیست سرویس‌ها را ببینید

3. **تست CRUD برای Expenses:**
   - یک هزینه اضافه کنید
   - لیست هزینه‌ها را ببینید

## 🎯 نکات مهم

- بعد از تغییر `.env`، حتماً سرور dev را restart کنید
- برای تست در production، باید `VITE_BACKEND_TYPE=supabase` را در build هم تنظیم کنید
- همیشه از `anon` key استفاده کنید، نه `service_role` key


# 🔧 رفع خطای SSL Error 525 در Cloudflare

## 📋 مشکل

وقتی وارد آدرس `https://app.khodroban.top/` می‌شوید، خطای زیر نمایش داده می‌شود:

```
SSL handshake failed
Error code 525
```

## 🔍 علت مشکل

خطای 525 به این معنی است که **Cloudflare نمی‌تواند با سرور Origin (Netlify) یک اتصال SSL/TLS برقرار کند**.

این مشکل معمولاً به دلایل زیر رخ می‌دهد:

1. **SSL/TLS Mode در Cloudflare روی "Full" یا "Full (strict)" تنظیم شده** اما سرور origin نمی‌تواند SSL handshake را کامل کند
2. سرور Origin (Netlify) در دسترس نیست یا مشکل دارد
3. تنظیمات DNS اشتباه است

## ✅ راه‌حل‌ها

### راه‌حل ۱: تغییر SSL/TLS Mode در Cloudflare (توصیه می‌شود)

1. **وارد داشبورد Cloudflare شوید**
   - به https://dash.cloudflare.com بروید
   - دامنه `khodroban.top` را انتخاب کنید

2. **به بخش SSL/TLS بروید**
   - از منوی سمت چپ، **SSL/TLS** را انتخاب کنید
   - روی تب **Overview** کلیک کنید

3. **SSL/TLS encryption mode را تغییر دهید**
   - گزینه **"Flexible"** را انتخاب کنید
   - این گزینه به Cloudflare اجازه می‌دهد با سرور origin بدون نیاز به SSL certificate معتبر ارتباط برقرار کند

4. **صبر کنید تا تغییرات اعمال شود**
   - معمولاً ۱-۲ دقیقه طول می‌کشد
   - بعد از آن، سایت باید در دسترس باشد

### راه‌حل ۲: بررسی وضعیت Netlify

مطمئن شوید که سایت روی Netlify به درستی deploy شده است:

1. **بررسی داشبورد Netlify**
   - به https://app.netlify.com بروید
   - پروژه خود را پیدا کنید
   - مطمئن شوید که آخرین deploy با موفقیت انجام شده است

2. **بررسی URL اصلی Netlify**
   - URL اصلی Netlify (مثلاً `https://your-site.netlify.app`) را بررسی کنید
   - اگر این URL کار می‌کند، مشکل از Cloudflare است
   - اگر این URL هم کار نمی‌کند، مشکل از Netlify است

### راه‌حل ۳: بررسی تنظیمات DNS

1. **در Cloudflare، به بخش DNS بروید**
   - مطمئن شوید که یک رکورد **CNAME** برای `app.khodroban.top` وجود دارد
   - این رکورد باید به URL Netlify شما اشاره کند (مثلاً `your-site.netlify.app`)
   - یا اگر از custom domain در Netlify استفاده می‌کنید، باید به همان اشاره کند

2. **مثال تنظیمات DNS:**
   ```
   Type: CNAME
   Name: app
   Target: your-site.netlify.app
   Proxy status: Proxied (orange cloud)
   ```

### راه‌حل ۴: استفاده از Full (strict) با SSL Certificate معتبر

اگر می‌خواهید از **Full (strict)** استفاده کنید:

1. **در Netlify:**
   - مطمئن شوید که SSL certificate در Netlify فعال است
   - Netlify به صورت خودکار SSL certificate برای دامنه‌هایش فراهم می‌کند

2. **در Cloudflare:**
   - بعد از اینکه مطمئن شدید Netlify از SSL پشتیبانی می‌کند
   - SSL/TLS mode را به **"Full (strict)"** تغییر دهید

## 🎯 تنظیمات پیشنهادی

برای یک تنظیمات امن و پایدار:

### مرحله ۱: شروع با Flexible
```
SSL/TLS Mode: Flexible
```

### مرحله ۲: بعد از اطمینان از کارکرد، تغییر به Full
```
SSL/TLS Mode: Full
```

### مرحله ۳: برای امنیت بیشتر، استفاده از Full (strict)
```
SSL/TLS Mode: Full (strict)
```

## 🔍 بررسی و تست

بعد از اعمال تغییرات:

1. **صبر کنید ۱-۲ دقیقه** تا تغییرات اعمال شود
2. **Cache را پاک کنید:**
   - در Cloudflare: **Caching > Configuration > Purge Everything**
   - یا در مرورگر: `Ctrl+Shift+R` (Windows/Linux) یا `Cmd+Shift+R` (Mac)
3. **سایت را دوباره باز کنید**

## ⚠️ نکات مهم

- **Flexible mode** امنیت کمتری دارد اما برای شروع مناسب است
- **Full mode** امنیت بیشتری دارد و توصیه می‌شود
- **Full (strict)** بالاترین سطح امنیت را دارد اما نیاز به SSL certificate معتبر در origin server دارد

## 📞 در صورت ادامه مشکل

اگر بعد از اعمال این راه‌حل‌ها مشکل همچنان باقی بود:

1. **لاگ‌های Cloudflare را بررسی کنید**
   - داشبورد Cloudflare > Analytics & Logs > Events
   
2. **بررسی Status Page:**
   - https://www.cloudflarestatus.com/
   - https://www.netlifystatus.com/

3. **بررسی تنظیمات Origin Server:**
   - مطمئن شوید که Netlify از custom domain پشتیبانی می‌کند
   - در Netlify: Domain settings > Custom domains > Add custom domain

---

**آخرین بروزرسانی:** 2024


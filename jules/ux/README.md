# 🎨 UX Design Prototypes - KhodroBan

این پوشه شامل طراحی‌های UI/UX و نمونه‌های استاتیک HTML برای پروژه خودروبان است.

## 📁 ساختار پوشه‌ها

```
ux/
├── auth/                    # صفحات احراز هویت
│   ├── login/              # صفحه ورود
│   │   ├── index.html
│   │   └── preview.png
│   └── registration/        # صفحه ثبت‌نام
│       ├── index.html       # کد HTML
│       └── preview.png      # اسکرین‌شات
│
├── dashboard/               # صفحات داشبورد
│   └── variant-01/         # واریانت ۱
│
├── vehicles/                # صفحات مدیریت خودرو
│   ├── list/               # لیست خودروها
│   └── details/            # جزئیات خودرو
│
├── services/                # صفحات مدیریت سرویس
│   └── add-service/        # فرآیند افزودن سرویس
│       ├── new-service/    # صفحه اصلی افزودن سرویس
│       └── select-type/    # انتخاب نوع سرویس
│
├── expenses/                # صفحات مدیریت هزینه
│   └── add-expense/        # افزودن هزینه (در حال توسعه)
│
├── reminder/                # صفحات یادآورها
│   └── variant-02/         # واریانت ۲ یادآورها
│       ├── index.html
│       └── preview.png
│
├── report/                  # صفحات گزارش‌ها
│   └── details/            # جزئیات گزارش
│       ├── index.html
│       └── preview.png
│
├── settings/                # صفحه تنظیمات
│   ├── index.html
│   └── preview.png
│
├── upgrade/                 # صفحات ارتقا حساب
│   └── payment/            # صفحه پرداخت و ارتقا به Pro
│
└── smart-advisor/          # صفحه مشاور هوشمند
    ├── index.html
    └── preview.png
```

## 📝 قوانین نام‌گذاری

### پوشه‌ها
- استفاده از **kebab-case** (خط تیره برای جدا کردن کلمات)
- نام‌گذاری به **انگلیسی** و معنا دار
- مثال: `add-service`, `vehicle-details`, `smart-advisor`

### فایل‌ها
- `index.html` - کد HTML صفحه
- `preview.png` - اسکرین‌شات صفحه

### مراحل (Steps)
- برای فرآیندهای چند مرحله‌ای از `step-XX-description` استفاده می‌شود
- مثال: `step-01-select-type`, `step-02-enter-details`

## 🎯 دسته‌بندی صفحات

### 1. Authentication (احراز هویت)
- **login**: صفحه ورود کاربر
- **registration**: صفحه ثبت‌نام کاربر

### 2. Dashboard (داشبورد)
- **variant-01**: طراحی داشبورد

### 3. Vehicles (خودروها)
- **list**: لیست تمام خودروهای کاربر
- **details**: صفحه جزئیات یک خودرو

### 4. Services (سرویس‌ها)
- **add-service**: فرآیند افزودن سرویس جدید
  - **new-service**: صفحه اصلی افزودن سرویس/هزینه
  - **select-type**: انتخاب نوع سرویس

### 5. Expenses (هزینه‌ها)
- **add-expense**: افزودن هزینه جدید

### 6. Reminder (یادآورها)
- **variant-02**: صفحه یادآورها و اعلان‌ها

### 7. Settings (تنظیمات)
- تنظیمات حساب کاربری و اعلان‌ها

### 8. Upgrade (ارتقا)
- **payment**: صفحه پرداخت و ارتقا به نسخه Pro

### 9. Reports (گزارش‌ها)
- **details**: صفحه جزئیات گزارش

### 10. Smart Advisor (مشاور هوشمند)
- صفحه مشاوره و پیشنهادات هوشمند

## 🛠️ نحوه استفاده

### مشاهده طراحی‌ها
```bash
# باز کردن فایل HTML در مرورگر
open ux/dashboard/variant-01/index.html
```

### ویرایش طراحی‌ها
1. فایل `index.html` را در ویرایشگر باز کنید
2. تغییرات را اعمال کنید
3. اسکرین‌شات جدید بگیرید و `preview.png` را به‌روزرسانی کنید

### افزودن صفحه جدید
1. پوشه جدید در دسته‌بندی مناسب ایجاد کنید
2. فایل `index.html` و `preview.png` را اضافه کنید
3. این README را به‌روزرسانی کنید

## 📋 چک‌لیست

- [x] ساختار پوشه‌ها منظم شده
- [x] نام‌گذاری یکپارچه (kebab-case)
- [x] پوشه‌های تکراری حذف شده
- [x] فایل‌ها به نام‌های استاندارد تغییر کرده‌اند
- [ ] تمام صفحات دارای `index.html` و `preview.png` هستند

## 🔗 لینک‌های مرتبط

- [Frontend Project](../frontend/README.md)
- [Design System Documentation](./docs/design-system.md) (آینده)


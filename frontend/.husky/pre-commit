#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# اجرای lint-staged برای فایل‌های staged
npx lint-staged

# اجرای تست‌های smoke (سریع - کمتر از 2 ثانیه)
npm run test:smoke

# اجرای type checking
npm run check
